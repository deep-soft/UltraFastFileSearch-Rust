//! Search functions — re-exported from `uffs-core`.
//!
//! The actual search implementations now live in `uffs_core::search::query`.
//! This module exists only for backward compatibility with TUI internal
//! imports via `crate::search::*`.
