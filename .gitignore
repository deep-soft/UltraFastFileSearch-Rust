# SPDX-License-Identifier: MPL-2.0
# Copyright (c) 2025-2026 SKY, LLC.

# =============================================================================
# Rust / Cargo
# =============================================================================
debug/
target/
Cargo.lock
**/*.rs.bk
*.pdb
# macOS sed -i backup files
*.toml-e
*.yml-e
*.rs-e

# =============================================================================
# Trash / quarantine (local rollback staging)
# =============================================================================
/_trash/*
!/_trash/.gitignore
/_trash/**/.gitkeep
/_trash/**/.keep

# =============================================================================
# Build artifacts & generated binaries
# =============================================================================
build/
*.rlib
*.bin
/test_scf

# =============================================================================
# Logs, vendor, misc
# =============================================================================
/Shell/
# /LOG/ holds local-only runtime logs (defender ETLs, mental_health, etc.)
# but the per-iteration `*CHANGELOG_HEALING.md` files document each
# `just ship -v` recovery and ARE tracked.  Using `/LOG/*` (not `/LOG/`)
# keeps the directory itself unignored so the negation pattern below
# can re-include the changelog files.
/LOG/*
!/LOG/*CHANGELOG_HEALING.md
!/LOG/.gitkeep
/vendor/
/missing_paths.txt
/_trash/
/reference/
# Binaries are distributed via GitHub Releases, not committed to the repo
dist/
# Transient pipeline / debug logs captured at the workspace root via `tee`.
# Kept around between healing iterations but never committed.
/.ship_run*.log
/.clippy_run*.log
/.git_status.txt
/.git_show.txt
/.git_lsfiles.txt

# =============================================================================
# IDE / Editor / AI
# =============================================================================
.idea/
.vscode/
.ai/
.claude/
.qodo/
.reuse/
.windsurf/
*.iws
*.iml

# =============================================================================
# OS artifacts
# =============================================================================
# Windows
Thumbs.db
ehthumbs.db
Desktop.ini
# macOS
.DS_Store
.AppleDouble
.LSOverride
# Linux
*~

# =============================================================================
# Environment and secrets
# =============================================================================
.env
*.env

# =============================================================================
# Cargo metadata & local-only cargo config
# =============================================================================
metadata.json
.cargo/macos.toml
.cargo/windows.toml
.cargo/.zshrc

# =============================================================================
# Internal dev docs (plans, audits, investigations — local only)
# =============================================================================
/docs/dev/
/docs/research/
/docs/refactor/

# =============================================================================
# Local-only spec/prompt files
# =============================================================================
/prompts/
INTENT_PROMPT.md
INTENT_PROMPT_V2.md
INTENT_SPEC_FAANG_CORE_INFRA.mdconsole
crates/uffs-cli/console
